const SectionTitle = ({ title }) => (
  <h3 className="uppercase text-[20px] md:text-[25px] font-bold  leading-[24px] md:leading-[30px] text-center text-black">{title}</h3>
);

export default SectionTitle;
